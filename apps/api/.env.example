# ============================================================
# DevChrono JSONLab API - Environment Variables
# Copy to .env and configure
# ============================================================

NODE_ENV=development
PORT=3001
HOST=0.0.0.0

# CORS - comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:4001

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_JSON_MAX=30

# Request body limits
BODY_LIMIT_DEFAULT=1mb
BODY_LIMIT_JSON=10mb

# Logging
LOG_LEVEL=info

# Optional Redis (for distributed rate limiting)
# REDIS_URL=redis://localhost:6379

# Optional PostgreSQL (for user accounts)
# DATABASE_URL=postgresql://user:password@localhost:5432/devchrono

# API Version
API_VERSION=v1
